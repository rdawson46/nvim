local codewindow = require('codewindow')
codewindow.setup({
    auto_enable = false,
    max_line = 75,
    relative = 'editor',
    minimap_width = 5,
    max_minimap_height = 25,
})

codewindow.apply_default_keybinds()
